{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://sandboxie.local/schemas/srev-266-file-id-volume-scope-contract.schema.json",
  "id": "FILE_ID_VOLUME_SCOPE_CONTRACT",
  "title": "File ID volume scope contract",
  "type": "object",
  "required": [
    "owner",
    "contracts",
    "official_references",
    "runtime_gate"
  ],
  "properties": {
    "owner": {
      "const": "Sandboxie/core/dll/file.c"
    },
    "contracts": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 5
    },
    "official_references": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uri"
      },
      "minItems": 2
    },
    "runtime_gate": {
      "type": "string"
    }
  },
  "owner": "Sandboxie/core/dll/file.c",
  "contracts": [
    "File IDs are scoped to the file system volume that assigned them",
    "Open-by-id object names are binary reference numbers not Unicode path strings",
    "Boxed FileIds returned through file information paths are scrambled only so the paired File_GetName_FromFileId route can distinguish sandbox-volume IDs from real-volume IDs",
    "File_GetName_FromFileId first tries a true parent directory for boxed roots then falls back to the caller root and scrambled-ID retry",
    "This SREV does not change XOR shape query classes access masks share flags or open-by-id policy"
  ],
  "official_references": [
    "https://learn.microsoft.com/en-us/windows-hardware/drivers/ddi/ntifs/ns-ntifs-_file_internal_information",
    "https://learn.microsoft.com/en-us/windows-hardware/drivers/ddi/ntifs/nf-ntifs-ntcreatefile"
  ],
  "runtime_gate": "Windows file-id matrix with boxed and unboxed roots on distinct volumes, same numeric FileId collision simulation where possible, and FileInternalInformation/FileAllInformation callers followed by open-by-id retry"
}
